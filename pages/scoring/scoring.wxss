.page { padding-bottom: calc(120rpx + env(safe-area-inset-bottom)); }
.action-bar { position: fixed; left: 0; right: 0; bottom: 0; padding: 16rpx 24rpx calc(16rpx + env(safe-area-inset-bottom)); background: #fff; box-shadow: 0 -8rpx 24rpx rgba(0,0,0,.04); }
.num { text-align: right; font-variant-numeric: tabular-nums; }
.btn[disabled] { opacity: .45; }